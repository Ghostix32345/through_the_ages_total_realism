#generate through code
cb_war_from_event = {
    create_visible = {
        always = no
    }
    war_goal_type = superiority
}

cb_none = {
    declare_enabled = {
        can_declare_no_cb_war_on = scope:target
        OR = {
			AND = {
				relative_strength = {
					target = scope:target
					value >= 0.6
				}
				stability >= 20
			}
			is_human = yes
        }
    }
	
	no_cb = yes
	war_goal_type = superiority
}

cb_subject_broke_free = {
    create_visible = {
        always = no
    }
    declare_enabled = {
        OR = {
			AND = {
				relative_strength = {
					target = scope:target
					value >= 0.6
				}
				stability >= 20
			}
			is_human = yes
        }
    }
    war_goal_type = superiority
}

cb_insulted_us = {
    create_visible = {
        always = no
    }
    declare_enabled = {
        OR = {
			AND = {
				relative_strength = {
					target = scope:target
					value >= 0.6
				}
				stability >= 20
			}
			is_human = yes
        }
    }
    war_goal_type = superiority
}

cb_rebel_support = {
    create_visible = {
        always = no
    }
    declare_enabled = {
		is_human = yes
    }

    war_goal_type = superiority
}

cb_declined_call_to_arms = {
    create_visible = {
        always = no
    }
    declare_enabled = {
        OR = {
			AND = {
				relative_strength = {
					target = scope:target
					value >= 0.6
				}
				stability >= 20
			}
			is_human = yes
        }
    }

    war_goal_type = superiority
}

cb_denied_military_access = {
    create_visible = {
        always = no
    }
    declare_enabled = {
        OR = {
			AND = {
				relative_strength = {
					target = scope:target
					value >= 0.6
				}
				stability >= 20
			}
			is_human = yes
        }
    }

    war_goal_type = demand_military_access
}
cb_following_through_on_threat = {
    cut_down_in_size_cb = yes
	ai_subjugation_desire = -100
	ai_cede_location_desire = -100
	allow_release_areas = yes
    create_visible = {
        always = no
    }
    province = {
        always = yes #the province is assigned via the demand_unlawful_territory interaction anyways
    }
    declare_enabled = {
        OR = {
			AND = {
				relative_strength = {
					target = scope:target
					value >= 0.6
				}
				stability >= 20
			}
			is_human = yes
        }
    }
    war_goal_type = conquer_province

}


cb_destroyed_building = {
    create_visible = {
        always = no
    }
    declare_enabled = {
        OR = {
			AND = {
				relative_strength = {
					target = scope:target
					value >= 0.6
				}
				stability >= 20
			}
			is_human = yes
        }
    }
    war_goal_type = superiority
}