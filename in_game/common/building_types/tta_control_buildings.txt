governor_general = {
	audio_tier = 2
	is_foreign = no
	pop_type = nobles
	max_levels = 1
	important_for_UI = yes

	employment_size = large_noble_employment

	category = government_category

	rural_settlement = no
	town = yes
	city = yes
	megalopolis = no

	allow = {
		trigger_if = {
			limit = {
				scope:actor = {
					modifier:num_governor_generals <= 0
				}
			}
			custom_tooltip = {
				text = "Wir können noch keinen Generalgouverneur ernennen."
				always = no
			}
		}
		trigger_else = {
			custom_tooltip = {
				text = "Wir können keinen weiteren Generalgouverneur ernennen."
				scope:actor = {
					total_building_levels_including_construction:governor_general < modifier:num_governor_generals
				}
			}
		}
		owner = scope:actor #not in subjects
	}

	location_potential = {
		NOT = { is_connected_to_through_realm = owner.capital }
		is_overseas_for_owner = yes
		is_port = yes
	}

	remove_if = {
		OR = {
			location = { is_connected_to_through_realm = owner.capital }
			scope:actor = {
				total_building_levels_including_construction:governor_general > modifier:num_governor_generals
			}
			location = { is_overseas_for_owner = no }
		}
	}

	expensive = yes

	automation_build_allowed = no
	ai_forbid_shutdown = yes
	forbidden_for_estates = yes
	important_for_AI = yes

	build_time = government_build_time

	unique_production_methods = {
		naval_governor_maintenance = {
			tools = 1
			masonry = 1
			weaponry = 0.5
			paper = 3
			books = 1
			goods_gold = 1
			furniture = 2
			naval_supplies = 0.5
			fine_cloth = 0.5

			category = building_maintenance
		}
	}

	construction_demand = local_governor_construction

	modifier = {
		local_proximity_source = 100
		local_max_control = 1.0
	}
}

colonial_governor = {
	audio_tier = 2
	is_foreign = no
	pop_type = nobles
	max_levels = 1
	important_for_UI = yes
	
	employment_size = large_noble_employment
	
	category = government_category
	
	rural_settlement = no
	town = yes
	city = yes
	megalopolis = no

	allow = {
		trigger_if = {
			limit = {
				scope:actor = {
					modifier:num_governor_generals <= 0
				}
			}
			custom_tooltip = {
				text = "Wir können noch keinen Gouverneure ernennen."
				always = no
			}
		}
		trigger_else = {
			custom_tooltip = {
				text = "Wir können keinen weiteren Gouverneure ernennen."
				scope:actor = {
					total_building_levels_including_construction:colonial_governor < modifier:num_colonial_governors
				}
			}
		}
		owner = scope:actor #not in subjects
	}

	location_potential = {
		NOT = { is_connected_to_through_realm = owner.capital }
		NOT = { has_building_with_at_least_one_level = governor_general }
		is_overseas_for_owner = yes
	}

	remove_if = {
		OR = {
			location = { is_connected_to_through_realm = owner.capital }
			scope:actor = {
				total_building_levels_including_construction:colonial_governor > modifier:num_colonial_governors
			}
			location = { is_overseas_for_owner = no }
			location = { has_road_to_capital = yes }
		}
	}

	expensive = yes
	
	automation_build_allowed = no
	ai_forbid_shutdown = yes
	forbidden_for_estates = yes
	important_for_AI = yes
	ai_ignore_maintenance = yes

	build_time = government_build_time

	unique_production_methods = {
		colonial_governor_maintenance = {
			tools = 1
			masonry = 1
			weaponry = 0.5
			paper = 3
			books = 1
			goods_gold = 1
			furniture = 2
			fine_cloth = 1

			category = building_maintenance
		}
	}

	construction_demand = local_governor_construction

	modifier = {
		local_proximity_source = 80
	}
}