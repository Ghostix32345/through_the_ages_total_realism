
road_building = {
	age = age_1_traditions
	icon = road_building
	research_cost = 2.0
	has_road_building = yes
	
	unlock_road_type = gravel_road

	requires = city_building_advance
}

paved_road_advance = {
	age = age_3_discovery
	unlock_road_type = paved_road
	requires = crown_power_advance_discovery

	ai_weight = { add = 100 }
}

modern_road_advance = {
	age = age_5_absolutism
	unlock_road_type = modern_road
	requires = scientific_mapping_advance

	ai_weight = { add = 100 }
}

railroad_construction = {
	age = age_7_nationalism
	icon = road_building
	requires = steam_engine_perfection
	unlock_road_type = railroad

	global_construction_speed = 0.2
	trade_range = 250

	ai_weight = { add = 100 }
}